{
    "name": "website-booklist",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "packageManager": "bun@1.3.3",
    "scripts": {
        "dev": "turbo run dev",
        "vercel": "turbo run build",
        "commit": "git pull && bun update && cd apps/web && bun update && cd ../api && bun update && cd ../../ && bun getSecurity.ts && bun run vercel && read -p 'Descriptions Commit: ' message && git add . && echo -n \"$message - \" && node getTime.js | xargs -I {} git commit -m \"$message - {}\" && git push"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "turbo": "^2.6.3"
    },
    "peerDependencies": {
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "next-seo": "^7.0.1"
    }
}
